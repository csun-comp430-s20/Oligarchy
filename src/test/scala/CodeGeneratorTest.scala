import java.io.File

import org.scalatest.funsuite.AnyFunSuite



class CodeGeneratorTest extends AnyFunSuite {


  test("Print the number 1")
  test("Print the number 2") // so on this is basic
  // printing of integers we can then skip to largest of int
  test("Print a true value")
  test("Print a false value")
  test("try an Integer assignment")// try every kind of assign
  // we have
  test("try addition") // try every math operation
  // its ok if we need to remove some
  test("try a math operation on variables")


}
